import type { Source } from '../../domain/library/types'

/**
 * Catálogo de fontes bibliográficas do dataset oficial V1.
 * Na V1, todos os perfis legados não possuem fonte atribuída (sources = []).
 */
export const OFFICIAL_SOURCES_V1: Source[] = []
